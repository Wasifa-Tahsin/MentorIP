import React from 'react';
import Header from '../Header';
import Footer from '../Footer';

const Root = () => {
    return (
        <div>
            <Header></Header>
            <Footer></Footer>
        </div>
    );
};

export default Root;